import Testing

/// Parent suite grouping all Slot tests. Child suites live in sibling files
/// via `extension SlotSuite`, rendering as a nested node in the Test Navigator.
@Suite("Slot")
enum SlotSuite {}
